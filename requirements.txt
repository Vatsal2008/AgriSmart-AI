# CPU inference (model/predict.py) and the live camera scans (server/live_camera*/). Python 3.11-3.13.
# On Linux, install the CPU build of torch first:
#   pip install torch==2.14.0 torchvision==0.29.0 --index-url https://download.pytorch.org/whl/cpu
torch==2.14.0
torchvision==0.29.0
timm==1.0.26
pillow==12.3.0
